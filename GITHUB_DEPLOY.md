# GitHub 部署完整指南

## 📋 部署前准备

### 文件检查清单
确保以下文件都在项目中：
- ✅ `index.html` - 主页面
- ✅ `styles.css` - 样式文件
- ✅ `imgs/` 文件夹 - 包含所有图片
- ✅ `.gitignore` - Git 忽略文件（已创建）
- ✅ `README.md` - 项目说明

## 🚀 方法一：使用 GitHub Desktop（最简单）⭐ 推荐

### 第一步：下载 GitHub Desktop
1. 访问 [https://desktop.github.com](https://desktop.github.com)
2. 下载并安装 GitHub Desktop
3. 使用你的 GitHub 账号登录

### 第二步：创建仓库
1. 打开 GitHub Desktop
2. 点击 **"File"** → **"New Repository"**（新建仓库）
3. 填写信息：
   - **Name**: `portfolio`（或任何你喜欢的名字）
   - **Description**: "Yue's Project Manager Portfolio"
   - **Local Path**: 选择你的项目文件夹位置
   - ✅ 勾选 **"Initialize this repository with a README"**（可选）
4. 点击 **"Create Repository"**

### 第三步：添加文件
1. GitHub Desktop 会自动检测到你的文件
2. 在左侧会显示所有未提交的文件
3. 在底部填写提交信息：
   - **Summary**: "Initial commit: Portfolio website"
   - **Description**: "Add portfolio website files"
4. 点击 **"Commit to main"**（提交到主分支）

### 第四步：发布到 GitHub
1. 点击右上角的 **"Publish repository"**（发布仓库）
2. 可以选择是否保持仓库为私有（Private）
3. 点击 **"Publish Repository"**
4. 等待上传完成

### 第五步：启用 GitHub Pages
1. 在 GitHub 网站上打开你的仓库
2. 点击 **"Settings"**（设置）
3. 在左侧菜单找到 **"Pages"**
4. 在 **"Source"** 部分：
   - 选择 **"Deploy from a branch"**
   - **Branch**: 选择 `main`（或 `master`）
   - **Folder**: 选择 `/ (root)`
5. 点击 **"Save"**

### 第六步：获取网站链接
1. 等待几分钟让 GitHub Pages 部署完成
2. 你的网站链接格式：
   ```
   https://yourusername.github.io/portfolio/
   ```
3. 在仓库的 **"Settings" → "Pages"** 页面可以看到你的链接

---

## 🖥️ 方法二：使用命令行（适合有经验的用户）

### 第一步：安装 Git
- **Mac**: Git 通常已预装，或从 [git-scm.com](https://git-scm.com) 下载
- **Windows**: 从 [git-scm.com](https://git-scm.com) 下载安装

### 第二步：在项目文件夹中打开终端
1. 打开终端（Terminal）
2. 导航到项目文件夹：
   ```bash
   cd /Users/yuequ/Desktop/Cursor_Portfolio
   ```

### 第三步：初始化 Git 仓库
```bash
git init
```

### 第四步：添加所有文件
```bash
git add .
```

### 第五步：提交文件
```bash
git commit -m "Initial commit: Portfolio website"
```

### 第六步：在 GitHub 创建仓库
1. 访问 [github.com](https://github.com)
2. 点击右上角 **"+"** → **"New repository"**
3. 填写仓库名称（如：`portfolio`）
4. **不要**勾选 "Initialize with README"（因为我们已经有了）
5. 点击 **"Create repository"**

### 第七步：连接本地仓库到 GitHub
```bash
git remote add origin https://github.com/yourusername/portfolio.git
```
（将 `yourusername` 和 `portfolio` 替换为你的实际信息）

### 第八步：推送文件到 GitHub
```bash
git branch -M main
git push -u origin main
```

### 第九步：启用 GitHub Pages
1. 在 GitHub 仓库页面，点击 **"Settings"**
2. 找到 **"Pages"**
3. 选择 **"Deploy from a branch"**
4. 选择 `main` 分支和 `/ (root)` 文件夹
5. 点击 **"Save"**

---

## 🌐 方法三：直接在 GitHub 网页上传

### 第一步：创建新仓库
1. 访问 [github.com](https://github.com)
2. 点击右上角 **"+"** → **"New repository"**
3. 填写仓库名称：`portfolio`
4. 选择 **Public**（公开）或 **Private**（私有）
5. **不要**勾选任何初始化选项
6. 点击 **"Create repository"**

### 第二步：上传文件
1. 在仓库页面，点击 **"uploading an existing file"**
2. 拖拽整个 `Cursor_Portfolio` 文件夹中的所有文件
   - `index.html`
   - `styles.css`
   - `imgs/` 文件夹（需要打开文件夹，选择所有图片）
3. 在底部填写提交信息：
   - **Commit message**: "Initial commit: Portfolio website"
4. 点击 **"Commit changes"**

### 第三步：启用 GitHub Pages
1. 点击 **"Settings"** → **"Pages"**
2. 选择 `main` 分支和 `/ (root)` 文件夹
3. 点击 **"Save"**

---

## ✅ 部署后检查

### 测试清单
- [ ] 仓库已创建并上传文件
- [ ] GitHub Pages 已启用
- [ ] 网站链接可以访问（格式：`https://yourusername.github.io/portfolio/`）
- [ ] 所有图片正常显示
- [ ] 样式正常加载
- [ ] 移动端显示正常
- [ ] 所有功能正常工作

### 常见问题解决

**问题1：网站显示 404**
- **解决**：等待 5-10 分钟让 GitHub Pages 完成部署
- 检查仓库名称和用户名是否正确

**问题2：图片不显示**
- **解决**：确保 `imgs` 文件夹和所有图片都已上传
- 检查图片路径是否正确（`imgs/文件名.png`）

**问题3：样式丢失**
- **解决**：确保 `styles.css` 文件已上传
- 检查文件路径是否正确

**问题4：更新后网站没有变化**
- **解决**：GitHub Pages 需要几分钟来更新
- 清除浏览器缓存后刷新

---

## 🔄 更新网站内容

### 使用 GitHub Desktop
1. 修改本地文件
2. 在 GitHub Desktop 中会显示更改
3. 填写提交信息
4. 点击 **"Commit to main"**
5. 点击 **"Push origin"** 推送到 GitHub
6. GitHub Pages 会自动更新（需要几分钟）

### 使用命令行
```bash
git add .
git commit -m "Update portfolio content"
git push
```

### 直接在 GitHub 网页
1. 在仓库中点击要编辑的文件
2. 点击铅笔图标（Edit）
3. 修改内容
4. 点击 **"Commit changes"**

---

## 🎨 自定义域名（可选）

如果你想使用自己的域名：

1. 在仓库 **Settings** → **Pages**
2. 在 **Custom domain** 输入你的域名
3. 按照提示配置 DNS 设置
4. 等待 DNS 生效（通常几分钟到几小时）

---

## 📱 移动端测试

部署后务必测试：
- 在手机上访问你的 GitHub Pages 链接
- 检查所有内容是否正常显示
- 测试横向滚动功能
- 检查响应式布局

---

## 🎉 完成！

你的网站链接格式：
```
https://yourusername.github.io/portfolio/
```

**恭喜！你的作品集网站已经上线了！** 🚀

---

## 💡 提示

- GitHub Pages 提供 **免费 HTTPS**
- 网站会自动优化
- 可以随时更新内容
- 支持自定义域名
- 完全免费使用

如有问题，可以查看 GitHub 官方文档或联系我！

